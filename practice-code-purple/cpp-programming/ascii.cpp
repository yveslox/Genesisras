#include<iostream>

using namespace std;

int main()
{
    char c;
    
    cout<<"=== program to find the ASCII value of a character ===\n";

    cout<<"Enter a character : ";
    cin>>c;
    
    cout<<"The ASCII value of the entered character is : "<<(int)c<<endl;
    
    return (0);
}